import SwiftUI

struct {{ argument.component }}View: ComponentView {
    @ObservedObject var viewModel: {{ argument.component }}ViewModel

    init(viewModel: {{ argument.component }}ViewModel) {
        self.viewModel = viewModel
    }

    var body: some View {
        Text("\(viewModel.title)")
    }
}

extension {{ argument.component }}View {
    static func == (lhs: {{ argument.component }}View, rhs: {{ argument.component }}View) -> Bool {
        lhs.viewModel.id == rhs.viewModel.id
    }
}
