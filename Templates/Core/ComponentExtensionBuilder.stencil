import Foundation

public extension Component {
{% for type in types.structs where type.annotations.component %}
    static func {{ type.annotations.component }}Component(properties: {{ type.name }}) -> Component {
        return Component(contentType: "{{ type.annotations.component }}",
                         properties: properties,
                         id: UUID())
    }
{% endfor %}
}
